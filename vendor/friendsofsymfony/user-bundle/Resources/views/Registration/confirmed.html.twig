{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <link type="text/css" rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap.min.css')}}">
    <link type="text/css" rel="stylesheet" href="{{ asset('css/album.css')}}">
    <div class="container" id="contant">
        <h2 id="h2color">Félicitation <span id="rouge">{{user.username}}</span>!!!</h2>

        <h3 id="h2color">Votre compte a été creer avec succes .</h3>
        <h3 id="rouge"><span id="spanconfirmed">Vous devriez acceder à la page avec votre adresse e-mail :</span> {{user.email}}</h3></br>

        <h4><a class="btn btn-outline-success my-2 my-sm-0" href="{{ path('liste_albums')}}">Vers l'album</a></h4>
        <h4><a class="btn btn-outline-primary my-2 my-sm-0" href="{{ path('liste_artiste')}}">Vers l'artiste</a></h4>
    </div>
{% endblock fos_user_content %}
